
# 构建一个基于艾宾浩斯遗忘曲线的单词记忆提醒工具（macOS）

## 1. 项目概述
你是一位资深的 macOS 开发者，请使用 **SwiftUI** 构建一个 **艾宾浩斯遗忘曲线单词记忆辅助工具**。该工具帮助用户追踪多个单词列表（List）的记忆进度，并根据遗忘曲线规律，在特定日期发送本地通知提醒用户复习对应的列表。

**核心原则：**
- 完全关闭程序后仍然能够通过系统本地通知进行提醒，无需后台常驻进程。
- 极致轻量，极少占用内存。
- 代码结构化、模块化，便于未来封装为 `.app` 发布。
- 当前阶段可直接通过 Xcode 运行，无需实际打包。

## 2. 技术与环境要求
- 平台：macOS 12+
- 界面框架：SwiftUI
- 生命周期：SwiftUI App 协议（`@main`）
- 通知：UserNotifications 框架（`UNUserNotificationCenter`）
- 数据持久化：使用 `UserDefaults` + `Codable` 存储记忆项目，不使用 Core Data 或第三方数据库，以保持轻量。
- 不使用任何第三方依赖。
- 遵循 MVVM 架构，ViewModel 作为状态管理中心。

## 3. 艾宾浩斯遗忘曲线规则
- 复习间隔（天数，相对于列表创建日）：
  - 第 1 天
  - 第 2 天
  - 第 4 天
  - 第 7 天
  - 第 15 天
  - 第 30 天
- 复习间隔硬编码，后续可扩展为自定义，但当前版本固定。
- **复习规则细化：**
  - 以列表的 **创建日期** 为第 0 天。
  - 第 1 天指创建后经过 1 个自然日，即创建日的次日。
  - 对于每个列表，计算其所有需复习的“目标日期”（如 2026-05-14、2026-05-15 ...）。
  - 在目标日期的 **上午 9:00** 触发提醒通知。
  - 用户可在目标日期及之后任意时间点开程序，对列表进行“复习标记”。
  - 标记后，该日期的复习任务视为完成，若未标记则在界面中显示为“逾期未复习”，优先级更高。

## 4. 数据模型设计
使用 Swift 结构体并遵循 `Codable`、`Identifiable`。

```swift
struct MemoryItem: Codable, Identifiable {
    var id: UUID
    var listName: String          // 用户为单词列表取的名称，如“List 1”
    var creationDate: Date        // 创建日期（用于计算复习日程）
    var reviewRecords: [ReviewRecord] // 记录各次复习的完成状态
}

struct ReviewRecord: Codable, Identifiable {
    var id: UUID
    var targetDate: Date          // 按遗忘曲线计算出的复习目标日期
    var isCompleted: Bool         // 用户是否已完成该次复习
}
```

**注意：**
- 创建 `MemoryItem` 时，自动根据 `creationDate` 生成 6 条 `ReviewRecord`，其 `targetDate` 分别为 `creationDate + 1天、2天、4天、7天、15天、30天`，`isCompleted` 默认为 `false`。
- `targetDate` 的时间分量统一设为当天的 09:00:00（用于通知触发时间）。
- 当用户标记某次复习为完成，相应 `ReviewRecord.isCompleted` 变为 `true`。

## 5. 通知机制（关键）
- **所有通知必须在列表创建或修改时提前安排，绝不依赖后台进程轮询。**
- 创建 `MemoryItem` 时，遍历其 `ReviewRecord` 数组，为每个 `targetDate` 大于当前时间的未完成记录安排一个 `UNNotificationRequest`。
- 通知 identifier 格式建议：`"\(memoryItem.id.uuidString)-\(reviewRecord.id.uuidString)"`
- 通知内容：
  - 标题：`“复习提醒”`
  - 正文：`“该复习「\(listName)」了”`
  - 声音：`UNNotificationSound.default`
  - 触发条件：`UNCalendarNotificationTrigger` 使用目标日期的年月日以及 9 点 0 分。
- **当用户将某条记录标记为已完成时：**
  - 移除对应的已安排通知（`UNUserNotificationCenter.current().removePendingNotificationRequests(withIdentifiers:)`）
- **当用户删除整个记忆项目时：**
  - 移除该项目所有相关通知。
- 程序启动时，应请求通知权限，并清理已经过去的通知（不影响数据库记录，仅移除已触发的通知）。
- 确保通知在程序完全退出后依然能正常触发。

## 6. 界面布局与交互设计
使用 **NavigationSplitView** 实现双栏布局（包含侧边栏与详情），适配 macOS 习惯。

### 侧边栏（列表区）
- 顶部标题“记忆项目”，右上角有个 `+` 按钮用于创建新列表。
- 列表展示所有 `MemoryItem`：
  - 每一项显示：
    - 列表名称
    - 状态标识：
      - **绿色圆点**：今日有未完成的复习（最高优先级）
      - **橙色圆点**：有已逾期未完成的复习
      - **灰色圆点**：暂无待复习
  - 点击某行，右侧显示该项目的详细复习计划。
- 支持侧滑或右键菜单删除项目，删除时需二次确认，同时移除相关通知。

### 详情区域
选中某个列表后显示：
- 列表名称（可点击编辑修改）
- 创建日期展示
- “复习进度”分段（可通过 `Picker` 或 Toggle 切换查看“全部/待复习/已完成”）
- 复习计划列表，每一条显示：
  - 日期（如“第1天：2026-05-14”）
  - 状态标签：“待复习”、“已完成”、“已逾期”
  - 如果“待复习”且日期为今天或已过期，应高亮显示，并提供“标记完成”按钮。
- “标记完成”按钮：
  - 点击后，对应 `ReviewRecord.isCompleted` 改为 `true`，同时移除对应通知，界面立刻反映变更。
- 如果存在逾期未复习的条目，在详情顶部显示醒目的提示条“有 N 条逾期复习，请尽快完成”。

### 创建列表弹窗
- 使用 Sheet 弹出表单：
  - 文本输入框：列表名称
  - 日期选择器：创建日期（默认为今天）
  - “创建”按钮，保存数据并安排通知，关闭 Sheet。
  - 输入校验：名称不能为空。

## 7. 数据持久化实现
- 使用 `UserDefaults` 存储 `[MemoryItem]` 数组，通过 `JSONEncoder/JSONDecoder` 转换。
- 存取 key 为 `"memoryItems"`。
- App 启动时加载数据，并处理可能的数据迁移和过期通知清理。
- 数据变更后立即调用存储方法（防崩溃可加 debounce，但由于轻量直接同步写入）。

## 8. 边缘情况与逻辑细节
- **复习日期计算：** 使用 `Calendar.current.date(byAdding: .day, value: N, to: creationDate)` 并将时间统一调整到当日 09:00。
- **日期判断：** 所有日期比较仅比较“年月日”，忽略具体时间。使用 `Calendar.current.compare(date1, to: date2, toGranularity: .day)`。
- **逾期判断：** `targetDate` 的日期 < 今天 且 `isCompleted == false`。
- **今日待复习判断：** `targetDate` 的日期 == 今天 且 `isCompleted == false`。
- **通知权限：** 启动时 `requestAuthorization`，若用户拒绝，可在界面上提示“通知权限未开启，将无法接收提醒”。
- **无后台进程要求：** 绝对不创建定时器或后台任务进行轮询，仅依赖系统调度通知。
- **删除整个 List 时的清理：** 必须同步移除所有 `Pending` 和未来 `Delivered`（虽不能撤回已推，但应删除标识）。

## 9. 代码结构与组织
- `MemoryItemModel.swift`：数据模型定义
- `PersistenceController.swift`：负责 UserDefaults 存取
- `NotificationManager.swift`：负责通知的调度、删除、权限请求
- `MainViewModel.swift`：遵循 `ObservableObject`，持有 `[MemoryItem]`，计算各种展示属性，监听变更并更新通知
- `ContentView.swift`：主界面，包含侧边栏与详情
- `CreateMemoryItemView.swift`：新建列表 Sheet 视图
- `DetailView.swift`：详情视图，显示复习计划及交互
- `MemoryItemRow.swift`：侧边栏行视图
- `艾宾浩斯遗忘曲线App.swift`：`@main` 入口

## 10. 额外行为（进阶可选，但建议实现）
- 若用户在某天错过了复习，在后续打开程序时，逾期条目自动排在待复习列表的最上方。
- 提供“一键补签”功能：允许用户一次性标记所有逾期条目为已完成，并清除相关通知。
- 每日复习提醒数在 App 图标角标上显示（用 `NSApplication.shared.dockTile.badgeLabel`），更新逻辑为今日待复习条目数。

## 11. 生成输出要求
请根据以上需求，一次性提供完整的、可直接运行的 Xcode 项目代码。代码中需要包含所有必要的文件，注释清晰，尤其对通知调度和日期计算部分加以详细说明。确保在完全关闭程序后，系统通知能按时触发。
```
